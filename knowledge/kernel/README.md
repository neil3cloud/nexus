# Kernel

## Status

Deprecated.

This directory has been superseded by `knowledge/specifications/` (normative WHAT) and `knowledge/reference/` (architectural HOW).

## Migration Guidance

- Use `knowledge/specifications/` for constitutional and RFC-defined kernel behavior.
- Use `knowledge/reference/` for service maps, dependency graphs, and implementation-facing architecture.
- Treat documents in this folder as historical context only.
