# Builder Task

## Source

Engineering Review Report:

`REVIEW_HISTORY.md` § `NEXUS-REV-2026-07-18-001`

Review Reference:

`NEXUS-REV-2026-07-18-001` (Sprint 76 — Approved Plan Activation; `BT-076-002` Resolution Verification; Overall Disposition: FAIL, unchanged in kind); originating cycle `NEXUS-REV-2026-07-17-020` (Overall Disposition: FAIL)

Sprint:

Sprint 76 — Approved Plan Activation (Milestone 11, Initial Capability Sequence step 7; authorized by `NEXUS-RAT-2026-07-17-017`, incorporating the typed `ReviewPlanRevisionReference` migration ratified by `NEXUS-RAT-2026-07-17-016` and completed by `NEXUS-RAT-2026-07-18-001`) — ❌ Rejected this cycle; remediation in progress.

Generated By:

`nexus-sprint`

Generation Date:

2026-07-17 (originating); updated 2026-07-18 (`BT-076-001` unblocked by `NEXUS-RAT-2026-07-18-001`); updated 2026-07-18 (`BT-076-002` marked Completed, verified by `NEXUS-REV-2026-07-18-001`)

---

# Executive Summary

The Reviewer's follow-up cycle (`NEXUS-REV-2026-07-18-001`) independently verified `BT-076-002`'s corrective implementation fully resolves `NEXUS-REV-2026-07-17-020-F-002`: Activation's executable `MissionPlan` commit now precedes the `ProposedPlanRevision` lifecycle persistence, with a new regression test confirming no stranded state on failure and successful retry afterward. `BT-076-002` is Completed and moved to Resolved Builder Tasks below.

`BT-076-001` (the typed `Review` call-site migration, unblocked by `NEXUS-RAT-2026-07-18-001` in the prior cycle) was not attempted this Builder pass and remains Open, unchanged in scope. Overall Disposition remains **FAIL**, unchanged in kind: Sprint 76's authorized scope is not yet fully delivered while `BT-076-001` remains outstanding. `IMPLEMENTATION_PLAN.md` remains unchanged, per the Reviewer's FAIL-disposition governance rule.

This document now contains **one Open Builder Task** (`BT-076-001`) and no Blocked Builder Task. No Documentation Task is generated.

Milestone 11 Initial Capability Sequence step 7 (Approved Plan Activation, Sprint 76) remains open and not yet Approved. Step 8 (Sprint 77) is not authorized and out of scope for this document.

---

# Scope

## Included

- `BT-076-001` — resolution of `NEXUS-REV-2026-07-17-020-F-001` (Category 3 — Specification Conflict), unblocked by `NEXUS-RAT-2026-07-18-001`; not yet attempted, carried forward unchanged.

## Excluded

- `BT-076-002` — `NEXUS-REV-2026-07-18-001` recorded no new finding; Resolved and moved to Resolved Builder Tasks below.
- Milestone 11 Initial Capability Sequence step 8 (Sprint 77: Autonomous Planning Integration Validation and Milestone 11 Closure) — not authorized; requires its own future Sprint Owner scope ratification via `nexus-plan`. No task is generated here.
- Any RFC-0012 Planning-domain Domain Event publication, AI-generated planning, Adapter/provider selection, workflow orchestration, or new Repository Policy mechanism — all remain deferred per the Sprint 76 record, untouched by this review cycle.
- All prior-cycle Category 6 Observations from Sprints 60–75 — carried forward under Future Improvements below for continued traceability; none reopened, none touched by this cycle.

---

# Open Builder Tasks

## BT-076-001 — Migrate the legacy `Review` call site to the typed `ReviewPlanRevisionReference` and remove the silent-inference fallback

**Status:** OPEN (unblocked by `NEXUS-RAT-2026-07-18-001`; previously Blocked)

**Summary:** `src/hosts/vscode/host-mission-workflow.ts:471` still constructs its `reviewService.startReview(...)` call with a bare string `missionPlanRevision` argument, relying on a silent string-to-`{ kind: 'ExecutableMissionPlan' }` inference path in `review.aggregate.ts`. The Sprint Owner has authorized the explicit migration of this one call site and withdrawn the silent-inference fallback entirely.

**Authority:** `NEXUS-RAT-2026-07-18-001` (authorizes the explicit Host migration and withdraws the silent-inference fallback, resolving `NEXUS-REV-2026-07-17-020-F-001`); `NEXUS-RAT-2026-07-17-016` (typed `ReviewPlanRevisionReference`; "legacy untyped values are not inferred silently"); `NEXUS-RAT-2026-07-17-017` (same rule, confirmed unchanged for Sprint 76).

**References:**

- Originating finding: `NEXUS-REV-2026-07-17-020-F-001` (Category 3 — Specification Conflict, Major).
- Unblocking ratification: `NEXUS-RAT-2026-07-18-001`.
- Sprint: Sprint 76 — Approved Plan Activation.
- RFC: RFC-0006 v1.1 — Engineering Assessment Model.

**Implementation Targets:**

- `src/hosts/vscode/host-mission-workflow.ts:471` (the one call site — this one line's argument construction only; no other change to this file is authorized).
- `src/kernel/review/review.aggregate.ts`, `review.contract.ts`, `review.types.ts` (remove the `ReviewPlanRevisionReference | string` union and the silent-inference branch).
- `test/hosts/vscode/host-mission-workflow.test.ts`, `test/hosts/vscode/host-mission-workflow-command-registration.test.ts`, `test/kernel/planning/planning-correlation.test.ts`, `test/kernel/review/review.aggregate.test.ts`, and any other test still constructing a bare-string `Review`/`ReviewSnapshot`/`StartReviewCommand`.
- `IMPLEMENTATION_REPORT.md` § Sprint 76.

**Required Changes:**

- Update `host-mission-workflow.ts:471` to pass `missionPlanRevision: { kind: 'ExecutableMissionPlan', revisionId: missionPlanRevisionId }` explicitly, in place of the bare string. No other change to `host-mission-workflow.ts` is authorized.
- Remove the `ReviewPlanRevisionReference | string` union from `CreateReviewInput.missionPlanRevision` (`review.aggregate.ts`) and `StartReviewCommand.missionPlanRevision` (`review.contract.ts`); each SHALL require `ReviewPlanRevisionReference` only.
- Remove `normalizeReviewPlanRevisionReference`'s `typeof reference === 'string'` branch; a bare string input SHALL be rejected with `InvalidReviewDefinitionError`, not coerced.
- Update every test currently exercising the string-coercion branch to construct the typed reference explicitly.
- Correct `IMPLEMENTATION_REPORT.md`'s Sprint 76 section so its claim that legacy callers were normalized to the explicit `kind` is accurate for every call site.
- No other change to `Review`'s lifecycle, `ReviewOutcome`, `Finding`, or any other RFC-0006 concept is authorized. No change to any Sprint 72–75 Planning domain file is authorized by this task.

**Acceptance Criteria:**

- `host-mission-workflow.ts:471` constructs the typed `ReviewPlanRevisionReference` explicitly; no bare string remains at this call site.
- `CreateReviewInput`/`StartReviewCommand` accept `ReviewPlanRevisionReference` only; a bare string is a compile-time type error at every call site.
- Constructing a `Review` with an invalid/malformed reference (including a bare string, if reached at a non-typed boundary) is rejected with `InvalidReviewDefinitionError`.
- No test in the repository constructs a `Review`, `ReviewSnapshot`, or `StartReviewCommand` with a bare-string `missionPlanRevision`.
- `IMPLEMENTATION_REPORT.md`'s Sprint 76 section is corrected.
- Every previously-passing Sprint 1–76 test continues to pass (updated only where this migration requires it).
- Repository-wide validation passes: TypeScript compile, ESLint, Vitest, esbuild, extension-host bundle build.

**Reviewer Evidence:** `NEXUS-REV-2026-07-17-020-F-001`, `REVIEW_HISTORY.md` § `NEXUS-REV-2026-07-17-020`; unblocked by `NEXUS-RAT-2026-07-18-001`; confirmed still outstanding by `NEXUS-REV-2026-07-18-001-F-001`.

---

# Blocked Builder Tasks

None. `BT-076-001` was unblocked by `NEXUS-RAT-2026-07-18-001` and now appears under Open Builder Tasks above.

---

# Documentation Tasks

None. Neither `NEXUS-REV-2026-07-17-020` nor `NEXUS-REV-2026-07-18-001` recorded an independent Category 4 — Documentation Drift finding; the `IMPLEMENTATION_REPORT.md` inaccuracy remains recorded as evidence within `BT-076-001`'s originating finding and is resolved as part of that task's Acceptance Criteria.

---

# Future Improvements

## `NEXUS-REV-2026-07-17-016` Observation F-003 — `Review.missionPlanRevision`'s dual semantics (superseded by ratification; underlying migration now tracked as `BT-076-001`)

Category 6, Observation, Informational (Sprint 75; carried forward). This Observation's underlying concern — `Review`'s revision reference lacking a domain discriminator — was addressed by `NEXUS-RAT-2026-07-17-016` (RFC-0006 v1.1, typed `ReviewPlanRevisionReference`). The Reviewer's Sprint 76 cycle (`NEXUS-REV-2026-07-17-020`) found the resulting migration incomplete for one legacy call site; that specific, actionable gap is now tracked as `BT-076-001` above, not as an open Observation. No further Builder Task generated from this Observation itself.

## `NEXUS-REV-2026-07-17-014` Observation F-002 — `Review.missionPlanRevision` now holds a non-executable `ProposedPlanRevisionId`, not an executable RFC-0001 `MissionPlan` revision reference

Category 6, Observation, Informational (Sprint 74; carried forward). Superseded by the typed `ReviewPlanRevisionReference` model (`NEXUS-RAT-2026-07-17-016`); the ambiguity this Observation described no longer exists at the type level. No Builder Task.

## `NEXUS-REV-2026-07-17-014` Observation F-003 — `PlanningCorrelationService` constructor silently default-constructs collaborators when not injected

Category 6, Observation, Informational (Sprint 74; carried forward). `PlanningActivationService`'s constructor (Sprint 76) follows the same default-construction pattern for its collaborators. Recommend removing all default parameters across the Planning domain in a future slice. No Builder Task.

## `NEXUS-REV-2026-07-17-011` Category 6 Observation — Lifecycle transitions mint a new `ProposedPlanRevision` rather than mutating state in place

Category 6, Observation, Informational (Sprint 72; carried forward, not touched by Sprint 73 through 76). No Builder Task generated by this document.

## `NEXUS-REV-2026-07-17-006` Observation O-001 — Sprint 62 integration harness exercises the legacy caller-supplied path, not the new production auto-subscription path

Category 6, Observation, Informational (Sprint 69; carried forward, not touched by Sprint 70 through 76). No Builder Task.

## `NEXUS-REV-2026-07-17-005` Observation O-001 — Redundant, unreachable `processedEventIds` duplicate-check

Category 6, Observation, Informational (Sprint 68; carried forward, not touched by Sprint 69 through 76). No Builder Task.

## `NEXUS-REV-2026-07-17-003` Observation O-001 — Test name overstates Workflow-position lookup coverage

Category 6, Observation, Informational (Sprint 67; carried forward, not touched by Sprint 68 through 76). No Builder Task.

## `NEXUS-REV-2026-07-17-003` Observation O-002 — Review-before-GovernanceDecision ordering invariant untested

Category 6, Observation, Informational (Sprint 67; carried forward, not touched by Sprint 68 through 76). No Builder Task.

## `NEXUS-REV-2026-07-17-001` Observation — Redundant `repository.save()` skip on the idempotent governance-gated no-op path

Category 6, Observation, Informational (Sprint 65; carried forward, not touched by Sprint 66 through 76). No Builder Task.

## `NEXUS-REV-2026-07-16-017` Observation — Incidental out-of-scope test-timeout stabilization

Category 6, Observation, Informational (Sprint 64; carried forward, not touched by Sprint 65 through 76). No Builder Task.

## `NEXUS-REV-2026-07-16-016` Observation 1 — `GovernanceStateProjectionService` replay-on-read is redundant with live subscription

Category 6, Observation, Informational (Sprint 63; carried forward, not touched by Sprint 65 through 76). No Builder Task.

## `NEXUS-REV-2026-07-16-016` Observation 2 — Unexercised `GovernanceStateProjectionDecisionSnapshot` diagnostic fields

Category 6, Observation, Informational (Sprint 63; carried forward, not touched by Sprint 65 through 76). No Builder Task.

## `NEXUS-REV-2026-07-16-012-F-001` — `assertCompletionPermitted` evaluated twice on the completion path (carried forward)

Category 6, Observation, Informational (Sprint 61; carried forward, not touched by Sprint 62 through 76). No Builder Task.

## `NEXUS-REV-2026-07-16-011-F-002` — Inconsistent indentation in `engineering-session.test.ts` (carried forward)

Category 6, Observation, Informational (Sprint 60; carried forward, not touched by Sprint 62 through 76). No Builder Task.

---

# Resolved Builder Tasks (carried forward for traceability)

## BT-076-002 — Correct Activation's write ordering to a single exclusive commit boundary

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-18-001`.

Originating finding: `NEXUS-REV-2026-07-17-020-F-002` (Category 1 — Implementation Defect, Major). Resolution independently verified: `activateExclusive` now commits the staged executable `MissionPlan` (`stagingRepository.commit()`) before persisting the `ProposedMissionPlan`'s `Activated`/`Superseded` lifecycle transition. A new regression test (`FailingCommitMissionRepository`, `'leaves ProposedPlanRevision lifecycle unchanged when the executable MissionPlan commit fails'`) confirms no stranded `Activated` state, no executable `MissionPlan` or events on a failed commit, and that a subsequent retry for the same revision succeeds and correctly reaches `Activated`. `MissionPlanningService`, `MissionPlan`, `Task`, and `TaskDependency` confirmed unmodified.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-075-003 — Add a direct unit test for `assertExistingGovernanceDecisionCanBeSuperseded`'s terminal-outcome rejection branch

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-019`.

Originating finding: `NEXUS-REV-2026-07-17-018-F-001` (Category 1 — Implementation Defect, Minor). Resolution independently verified: one new direct unit test added reproducing the non-atomic-write edge case (a `PlanningCorrelation` whose recorded `governanceDecisionId` already references a terminal `GovernanceDecision` while its `ProposedPlanRevision` remains `Under Review`), confirming rejection of a differing `governanceDecisionId` and that the recorded reference remains unchanged. Zero production source modification.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-075-001 — Correct `evaluateGovernance`'s Governance-outcome handling for `Deferred` and `Escalation Required`, per `NEXUS-RAT-2026-07-17-015`

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-018`. Not touched by `BT-075-003`.

Originating finding: `NEXUS-REV-2026-07-17-016-F-001` (Category 2 — Architectural Violation, Critical). Resolved by the Sprint Owner's governance decision `NEXUS-RAT-2026-07-17-015` and RFC-0012 v1.1; implementation independently verified Resolved.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-075-002 — Add direct unit tests for the extended `assertCurrentStateAllowsNewRevision` guard (`Withdrawn` and `Rejected` branches)

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-017`. Not touched by any later Sprint 75 corrective cycle.

Originating finding: `NEXUS-REV-2026-07-17-016-F-002` (Category 1 — Implementation Defect, Minor). Resolution independently verified.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-074-001 — Add direct aggregate-level unit tests for the `Submitted → Under Review` transition and its rejection from `Draft`/`Withdrawn`

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-015` [Sprint 74 review cycle]. Not touched by Sprint 75 or any of its corrective cycles.

Originating finding: `NEXUS-REV-2026-07-17-014-F-001` (Category 1 — Implementation Defect, Minor). Resolution independently verified and not reopened by any subsequent review.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-072-001 — Wire `PlanningPolicy.requireMissionId` to real, observable validation, or remove it

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-012`. Not touched by Sprint 73 through any Sprint 75 corrective cycle.

Originating finding: `NEXUS-REV-2026-07-17-011-F-001` (Category 1 — Implementation Defect, Minor). Resolution independently verified and confirmed still absent (`requireMissionId` unreferenced) by every subsequent review through `NEXUS-REV-2026-07-17-019`, none of which touched any Sprint 72 file.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-072-002 — Reconcile `PlanningDiagnosticCode` union with the codes actually produced

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-012`. Not touched by Sprint 73 through any Sprint 75 corrective cycle.

Originating finding: `NEXUS-REV-2026-07-17-011-F-002` (Category 1 — Implementation Defect, Minor). Resolution independently verified and confirmed still intact by every subsequent review through `NEXUS-REV-2026-07-17-019`, none of which touched any Sprint 72 file.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

---

# Builder Instructions

The Builder SHALL:

- implement `BT-076-001` exactly as authorized by `NEXUS-RAT-2026-07-18-001`: the one-line `host-mission-workflow.ts:471` migration, removal of the `ReviewPlanRevisionReference | string` union and silent-inference branch, and the associated test updates;
- preserve every previously Approved Sprint (1 through 75) exactly as certified;
- preserve `BT-076-002`'s Completed, verified correction to `planning-activation.service.ts` exactly as implemented;
- preserve every ratified rule in `NEXUS-RAT-2026-07-17-016`, `NEXUS-RAT-2026-07-17-017`, and `NEXUS-RAT-2026-07-18-001` exactly as authorized.

The Builder SHALL NOT:

- modify any Sprint 1–75 domain file, service, contract, or test beyond what `BT-076-001` explicitly authorizes;
- modify any part of `host-mission-workflow.ts` other than the one authorized call-site argument at line 471;
- modify `planning-activation.service.ts` or `planning-activation.service.test.ts` (Sprint 76, `BT-076-002`, Completed and verified) absent a new Reviewer finding;
- reinterpret any carried-forward Observation listed above;
- modify the Kernel Canon, any RFC, or any Ratification;
- modify any `src/adapters` file;
- treat this document as authorization to begin any Milestone 11 Initial Capability Sequence step 8 (Sprint 77) work.

---

# Expected Builder Output

- `host-mission-workflow.ts:471` migrated to the typed `ReviewPlanRevisionReference`; the `ReviewPlanRevisionReference | string` union and silent-inference branch removed from `review.aggregate.ts`/`review.contract.ts`/`review.types.ts`; affected tests updated (`BT-076-001`).
- An updated `IMPLEMENTATION_REPORT.md` Sprint 76 section describing the `BT-076-001` correction.
- A report of implementation outcome, per `IMPLEMENTATION_CONSTITUTION.md`'s Required Deliverables, covering `BT-076-001` only.
