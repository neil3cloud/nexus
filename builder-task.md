# Builder Task

## Source

Engineering Review Report:

`REVIEW_HISTORY.md` § `NEXUS-REV-2026-07-17-012`

Review Reference:

`NEXUS-REV-2026-07-17-012` (Sprint 72 — Planning Policy and Proposed Plan Foundation, BT-072-001/BT-072-002 Resolution Verification; Overall Disposition: PASS)

Sprint:

Sprint 72 — Planning Policy and Proposed Plan Foundation (Milestone 11, Initial Capability Sequence Step 3; authorized by `NEXUS-RAT-2026-07-17-010`)

Generated By:

`nexus-sprint`

Generation Date:

2026-07-17

---

# Executive Summary

The Reviewer certified Sprint 72's corrective cycle (`NEXUS-REV-2026-07-17-012`, verifying resolution of `BT-072-001` and `BT-072-002` from the prior `builder-task.md`) with an Overall Disposition of **PASS**. Sprint 72 is now fully closed — zero Category 1 (Implementation Defect), Category 2 (Architectural Violation), Category 3 (Specification Conflict), Category 4 (Documentation Drift), or Category 5 (Governance Decision Required) findings remain. `BT-072-001` and `BT-072-002` are both Completed, independently verified Resolved by the Reviewer. No new Category 6 (Observation) finding was recorded in this cycle.

This document contains **no Open Builder Tasks**, **no Blocked Builder Tasks**, and **no Documentation Tasks** arising from this review cycle.

Sprint 72 is fully closed. Milestone 11's Initial Capability Sequence step 3 is complete. Step 4 (Governed Plan Generation) requires its own future Sprint Owner scope ratification via a future `nexus-plan` cycle; this document authorizes no further Milestone 11 Sprint implementation.

---

# Scope

## Included

None. `NEXUS-REV-2026-07-17-012` recorded no Category 1–5 finding requiring a Builder Task, Documentation Task, or Blocked Builder Task.

## Excluded

- `NEXUS-REV-2026-07-17-011`'s Category 6 Observation (lifecycle transitions mint a new, incrementally-numbered `ProposedPlanRevision` rather than mutating a state field in place) — Informational; carried forward under Future Improvements below; not reopened, no task.
- All prior-cycle Category 6 Observations from earlier Sprints (Sprints 60–69), carried forward under Future Improvements below for continued traceability; none reopened, none touched by Sprint 71 or 72.
- Milestone 11 Initial Capability Sequence steps 4–6 (Governed Plan Generation; Plan Review, Governance, and Activation; Autonomous Planning Integration Validation) — not findings of this review cycle; each requires its own future Sprint Owner scope ratification via `nexus-plan`. No task is generated here.

---

# Open Builder Tasks

None. `NEXUS-REV-2026-07-17-012` recorded no Category 1 — Implementation Defect finding.

---

# Blocked Builder Tasks

None. `NEXUS-REV-2026-07-17-012` recorded no Category 2 — Architectural Violation, Category 3 — Specification Conflict, or Category 5 — Governance Decision Required finding.

---

# Resolved Builder Tasks (carried forward for traceability)

## BT-072-001 — Wire `PlanningPolicy.requireMissionId` to real, observable validation, or remove it

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-012`.

Originating finding: `NEXUS-REV-2026-07-17-011-F-001` (Category 1 — Implementation Defect, Minor): `PlanningPolicy.requireMissionId` was accepted, stored, and defaulted but never enforced, and was structurally incapable of any observable effect since `ProposedMissionPlan` already unconditionally requires a valid `missionId`.

Resolution, independently verified by `NEXUS-REV-2026-07-17-012`: `requireMissionId` was removed from `PlanningPolicyInput`, `PlanningPolicySnapshot`, `PlanningPolicy.create`, and `PlanningPolicy.toSnapshot`; `grep -rn "requireMissionId" src test` confirmed zero remaining references anywhere in the repository. `ProposedMissionPlan` continues to require a valid `missionId` directly, unchanged. Full repository validation (`tsc --noEmit`, ESLint, 637/637 tests, 92 files, `npm run build`, `npm run test:extension-host:build`) independently re-executed and passing.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

## BT-072-002 — Reconcile `PlanningDiagnosticCode` union with the codes actually produced

**Status:** COMPLETED — verified by `NEXUS-REV-2026-07-17-012`.

Originating finding: `NEXUS-REV-2026-07-17-011-F-002` (Category 1 — Implementation Defect, Minor): three of eight `PlanningDiagnosticCode` union members (`InvalidProposedMissionPlanDefinition`, `InvalidProposedPlanRevisionDefinition`, `InvalidProposalLifecycleTransition`) were declared but never constructed as a `PlanningDiagnostic` value.

Resolution, independently verified by `NEXUS-REV-2026-07-17-012`: `PlanningDiagnosticCode` was narrowed to exactly the five codes actually produced (`MissingProposedTaskReference`, `SelfDependency`, `DuplicateDependency`, `DependencyCycle`, `PlanningPolicyViolation`); `grep -rn "InvalidProposedMissionPlanDefinition|InvalidProposedPlanRevisionDefinition" src test` confirmed zero remaining references. No new production code path was added to manufacture the removed codes — the corresponding failure conditions continue to use their existing, already-tested exception types, exactly as the task required. Full repository validation independently re-executed and passing.

No further action. This task SHALL NOT generate additional Builder actions; it is closed.

---

# Documentation Tasks

None. `NEXUS-REV-2026-07-17-012` recorded no Category 4 — Documentation Drift finding.

---

# Future Improvements

## `NEXUS-REV-2026-07-17-011` Category 6 Observation — Lifecycle transitions mint a new `ProposedPlanRevision` rather than mutating state in place

Category

Observation

Summary

`ProposedMissionPlan.submitCurrentRevision`/`withdrawCurrentRevision` implement a lifecycle transition by constructing a new, incrementally-numbered `ProposedPlanRevision` carrying forward identical Proposed Task/Dependency content, rather than mutating a lifecycle-state field on the same revision object in place. Legitimate, RFC-0012-compliant design choice, not a defect.

Reason Excluded

Outside Sprint 72 scope; no RFC-0012 rule mandates the alternative. Flagged for the future Planning Correlation Sprint (Milestone 11 step 4/5), which will need to decide explicitly which `ProposedPlanRevisionId` — the originally-drafted one or the post-`Submitted`-transition one — is "the exact revision under evaluation" for Review/Governance purposes. No Builder Task generated by this document.

## `NEXUS-REV-2026-07-17-006` Observation O-001 — Sprint 62 integration harness exercises the legacy caller-supplied path, not the new production auto-subscription path

Category 6, Observation, Informational (Sprint 69; carried forward, not touched by Sprint 70, 71, or 72). No Builder Task.

## `NEXUS-REV-2026-07-17-005` Observation O-001 — Redundant, unreachable `processedEventIds` duplicate-check

Category 6, Observation, Informational (Sprint 68; carried forward, not touched by Sprint 69 through 72). No Builder Task.

## `NEXUS-REV-2026-07-17-003` Observation O-001 — Test name overstates Workflow-position lookup coverage

Category 6, Observation, Informational (Sprint 67; carried forward, not touched by Sprint 68 through 72). No Builder Task.

## `NEXUS-REV-2026-07-17-003` Observation O-002 — Review-before-GovernanceDecision ordering invariant untested

Category 6, Observation, Informational (Sprint 67; carried forward, not touched by Sprint 68 through 72). No Builder Task.

## `NEXUS-REV-2026-07-17-001` Observation — Redundant `repository.save()` skip on the idempotent governance-gated no-op path

Category 6, Observation, Informational (Sprint 65; carried forward, not touched by Sprint 66 through 72). No Builder Task.

## `NEXUS-REV-2026-07-16-017` Observation — Incidental out-of-scope test-timeout stabilization

Category 6, Observation, Informational (Sprint 64; carried forward, not touched by Sprint 65 through 72). No Builder Task.

## `NEXUS-REV-2026-07-16-016` Observation 1 — `GovernanceStateProjectionService` replay-on-read is redundant with live subscription

Category 6, Observation, Informational (Sprint 63; carried forward, not touched by Sprint 65 through 72). No Builder Task.

## `NEXUS-REV-2026-07-16-016` Observation 2 — Unexercised `GovernanceStateProjectionDecisionSnapshot` diagnostic fields

Category 6, Observation, Informational (Sprint 63; carried forward, not touched by Sprint 65 through 72). No Builder Task.

## `NEXUS-REV-2026-07-16-012-F-001` — `assertCompletionPermitted` evaluated twice on the completion path (carried forward)

Category 6, Observation, Informational (Sprint 61; carried forward, not touched by Sprint 62 through 72). No Builder Task.

## `NEXUS-REV-2026-07-16-011-F-002` — Inconsistent indentation in `engineering-session.test.ts` (carried forward)

Category 6, Observation, Informational (Sprint 60; carried forward, not touched by Sprint 62 through 72). No Builder Task.

---

# Builder Instructions

The Builder SHALL:

- take no implementation action arising from this review cycle — zero Open Builder Tasks, zero Blocked Builder Tasks, zero Documentation Tasks exist;
- preserve Sprint 72's fully Approved implementation exactly as certified, including both corrective fixes (`BT-072-001`, `BT-072-002`) verified by `NEXUS-REV-2026-07-17-012`;
- preserve every ratified rule in `NEXUS-RAT-2026-07-17-010` exactly as implemented;
- preserve every previously Approved Sprint (1 through 72) exactly as certified.

The Builder SHALL NOT:

- reintroduce `PlanningPolicy.requireMissionId` or any of the three removed `PlanningDiagnosticCode` union members without a new Sprint Owner ratification defining their effect;
- reinterpret any carried-forward Observation listed above;
- introduce any Milestone 11 Initial Capability Sequence step 4–6 concept (Planning Correlation's Review/Governance association, Activation, RFC-0001 conversion, event publication, workflow orchestration) as new capability;
- modify the Kernel Canon, any RFC, or any Ratification;
- modify any `src/hosts` or `src/adapters` file;
- modify `GovernanceDecision`, `Review`, `EngineeringDecisionCorrelation`, `RecoveryRequirement`, `WorkflowChain`, `WorkflowStep`, `EngineeringSession`, `Mission`, `MissionPlan`, `Task`, `TaskDependency`, or any Sprint 1–71 contract;
- treat this document as authorization to begin any further Milestone 11 work — the next Sprint's scoping is reserved for a future `nexus-plan` cycle.

---

# Expected Builder Output

Given zero Open, Blocked, or Documentation Tasks, the Builder SHALL:

- take no action;
- report that Sprint 72 is fully closed with no outstanding work;
- report the outstanding governance dependency: Milestone 11 Initial Capability Sequence step 4 (Governed Plan Generation) awaits a future `nexus-plan` cycle and Sprint Owner scope ratification.
